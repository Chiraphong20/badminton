# SmashPang — ระบบจัดการก๊วนแบดมินตัน & POS

> เอกสารสรุปภาพรวมระบบ สำหรับใช้พรีเซนต์/ตั้งต้นโปรเจกต์ใหม่

---

## 1. ระบบนี้คืออะไร

**SmashPang** เป็นระบบ SaaS สำหรับบริหารจัดการ **ก๊วนแบดมินตัน** แบบครบวงจร ตั้งแต่จัดคิว/จับคู่ผู้เล่นลงคอร์ด ไปจนถึงคิดเงินค่าคอร์ด ค่าลูก และขายของ (เครื่องดื่ม/ขนม) พร้อมสรุปยอดรายรับและประวัติย้อนหลัง ออกแบบมาให้เจ้าของสนาม/แอดมินก๊วนใช้แทนการจดกระดาษหรือ Excel

**Tech stack:** React 19 + TypeScript + Vite (frontend), Express + MySQL (backend), Tailwind CSS, deploy บน Vercel

---

## 2. ฟีเจอร์หลัก

### 🏸 จัดการคอร์ด
- เพิ่ม/จัดการจำนวนคอร์ด รองรับ 4 คนต่อคอร์ด
- **Auto Match** — จับคู่ทีมให้สมดุลอัตโนมัติตามระดับฝีมือ (Rank) ไม่ว่าจะเหลือที่ว่างกี่ที่
- เริ่มเกม → บันทึกเกม (Reset Court) → คิดค่าคอร์ด+ค่าลูกเข้าบัญชีผู้เล่นอัตโนมัติ
- แก้ไขประวัติเกมย้อนหลัง พร้อมคำนวณคืน/เรียกเก็บส่วนต่างอัตโนมัติ
- ยกเลิกเกม (Undo) คืนค่าลูก/ค่าสนามให้ผู้เล่นทุกคน

### 👥 จัดการสมาชิก
- เพิ่มผู้เล่นเข้าก๊วนประจำวัน
- ระบบ Rank (S1, S, P+, P, VIP1 ฯลฯ) พร้อม **Rank Memory** จดจำอัตโนมัติ
- ติดตามสถานะ: รอคิว / กำลังเล่น / พัก + เช็คอินเพื่อจัดลำดับคิว
- นำเข้ารายชื่อจากก๊วนเก่า หรือวางจาก Clipboard/Text

### 🛒 POS & ขายของ
- เพิ่ม/แก้ไขราคา/ลบสินค้า (ขนม เครื่องดื่ม ฯลฯ)
- สั่งซื้อผูกเข้าบิลผู้เล่นแต่ละคน แก้ไข/ลบรายการที่คีย์ผิดได้

### 💰 การเงิน & ชำระเงิน
- ติดตามยอดค้างชำระแยกประเภท (ค่าคอร์ด / ค่าลูก / ค่าขนม)
- รับชำระเงินสด, โอนเงิน, หรือช่องทางอื่น
- จ่ายรวมหลายคนในบิลเดียว หรือจ่ายบางส่วน

### 📊 แดชบอร์ด & สถิติ
- ภาพรวมเรียลไทม์: จำนวนเกม, รายได้รวม, จำนวนลูกที่ใช้, จำนวนผู้เล่น
- สรุปประวัติการรับเงิน

### 🧾 บันทึกและประวัติ
- ประวัติการตีรายเกม (ใครตีกับใคร คอร์ดไหน ใช้ลูกเท่าไหร่)
- ประวัติการชำระเงินทั้งหมด พร้อมหมายเหตุ/ช่องทาง
- ดึงข้อมูลย้อนหลังของวันก่อนๆ กลับมาดูได้ (Session History)
- ค้นหาประวัติผู้เล่นแบบ autocomplete + ค้นหาหลายชื่อพร้อมกัน (comma-separated)

### ⚙️ ระบบ & แบ็กเอนด์
- ตั้งค่าราคาสนาม/ลูกได้เอง
- รองรับ session ข้ามเที่ยงคืน (นับเป็นวันเดียวกัน)
- Auto-sync ขึ้นฐานข้อมูลทุก 2 นาที กันข้อมูลหายตอนเครื่องแครช
- เริ่มวันใหม่ / ล้างกระดาน / Factory Reset
- มีหน้าจอ **Queue View** แยกต่างหาก (`/?queue`) สำหรับเปิดจอทีวี/มอนิเตอร์โชว์คิวลูกค้าได้โดยไม่ต้อง login

---

## 3. จุดเด่น

- **ฟีเจอร์ตรงโจทย์ธุรกิจก๊วนแบดมินตันจริง** ไม่ใช่ template ทั่วไป — Auto Match ตาม Rank, คำนวณคืนเงินอัตโนมัติเมื่อแก้/ยกเลิกเกม, ค้นหาหลายชื่อพร้อมกัน ล้วนแก้ pain point ของการจดมือ/Excel
- ใช้ parameterized query ทุกจุดฝั่ง backend — ปลอดภัยจาก SQL injection แม้ endpoint จะเปิดกว้าง
- Auto-migrate schema ตอน boot + auto-sync กันข้อมูลหาย ทำให้ deploy ใหม่ลื่นไหล ไม่ต้อง manual migrate
- โครงสร้างเรียบง่าย ไม่ over-engineer เหมาะกับสเกลของสนามแบด/ก๊วนทั่วไป ต้นทุนรันระบบต่ำ (MySQL + Express เดียว)
- มี Docker setup พร้อม deploy (Dockerfile, docker-compose) และมี production instance ใช้งานจริงแล้ว

---

## 4. จุดด้อย / ความเสี่ยงที่ควรแก้ก่อนขาย

| ประเด็น | รายละเอียด | ความสำคัญ |
|---|---|---|
| **ไม่มี auth ฝั่ง backend** | PIN login เช็คแค่ฝั่ง client (`localStorage`) ส่วน API ทุกเส้น (`/api/master`, `/api/sync`, `/api/state` ฯลฯ) ไม่มี middleware ตรวจสิทธิ์เลย ใครรู้ URL ยิง POST ตรงๆ แก้ข้อมูล/ยอดเงินได้ | 🔴 สูงมาก — ต้องแก้ก่อนขายให้ลูกค้าจริง |
| **CORS เปิดกว้างสุด** | ไม่จำกัด origin เลย ยิ่งซ้ำเติมปัญหาข้อบน | 🔴 สูง |
| **Single-tenant** | Settings/ข้อมูลออกแบบมาสำหรับสนามเดียว ถ้าจะขายเป็น SaaS หลายลูกค้าต้องปรับ schema ให้แยกข้อมูลตาม tenant | 🟠 ต้องทำก่อนขายเป็น multi-customer |
| **Sync แบบ snapshot ทับทั้งก้อน** | ไม่มี lock ถ้าเปิดพร้อมกันหลายเครื่อง/แท็บมีโอกาสข้อมูลเขียนทับกัน (race condition) | 🟠 ปานกลาง |
| **App.tsx ไฟล์เดียว ~1,600 บรรทัด** | รวม logic เกือบทั้งแอป ดูแล/ต่อยอดยากขึ้นเรื่อยๆ | 🟡 หนี้ทางเทคนิค |
| **ไม่มี automated test** | มีแต่สคริปต์ debug มือ (`test-*.js`, `check-*.js`) ไม่ใช่ test suite จริง แก้โค้ดเสี่ยง regression | 🟡 ควรมีก่อนขยายทีม |

---

## 5. แนะนำก่อนนำไปทำขายเป็นโปรเจกต์ใหม่

1. **ใส่ auth จริงที่ backend** (เช่น session token/JWT ตรวจทุก endpoint) — ห้ามพึ่ง client-side PIN อย่างเดียว
2. **จำกัด CORS** ให้เหลือเฉพาะ origin ของแอปจริง
3. ถ้าจะขายเป็น SaaS หลายสนาม → ออกแบบ **multi-tenant schema** (เพิ่ม `tenant_id`/`branch_id` แยกข้อมูลแต่ละลูกค้า)
4. เพิ่ม **automated test** อย่างน้อยสำหรับ logic คิดเงิน (จุดที่พลาดแล้วกระทบเงินจริง)
5. แตก `App.tsx` เป็น hooks/modules ย่อยเพื่อรองรับการต่อยอดฟีเจอร์ในอนาคต

---

*เอกสารนี้สรุปจากการรีวิวโค้ดจริงในโปรเจกต์ ณ วันที่ 2026-08-09*
